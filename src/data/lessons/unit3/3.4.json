{
  "id": "3.4",
  "title": "State and remember",
  "type": "coding",
  "phase1": {
    "code": "// ---- Basic state ----\n@Composable\nfun Counter() {\n    // State variable\n    var count by remember { mutableStateOf(0) }\n\n    Column(\n        modifier = Modifier.padding(16.dp),\n        horizontalAlignment = Alignment.CenterHorizontally\n    ) {\n        Text(\n            text = \"Count: $count\",\n            fontSize = 24.sp\n        )\n\n        Row(\n            horizontalArrangement = Arrangement.spacedBy(8.dp)\n        ) {\n            Button(onClick = { count-- }) {\n                Text(\"-\")\n            }\n            Button(onClick = { count++ }) {\n                Text(\"+\")\n            }\n            Button(onClick = { count = 0 }) {\n                Text(\"Reset\")\n            }\n        }\n    }\n}\n\n// ---- State with multiple variables ----\n@Composable\nfun UserProfile() {\n    var name by remember { mutableStateOf(\"Kotlin-chan\") }\n    var age by remember { mutableStateOf(25) }\n    var isEditing by remember { mutableStateOf(false) }\n\n    Column(\n        modifier = Modifier.padding(16.dp)\n    ) {\n        if (isEditing) {\n            // Edit mode\n            OutlinedTextField(\n                value = name,\n                onValueChange = { name = it },\n                label = { Text(\"Name\") }\n            )\n            OutlinedTextField(\n                value = age.toString(),\n                onValueChange = { age = it.toIntOrNull() ?: age },\n                label = { Text(\"Age\") }\n            )\n            Button(onClick = { isEditing = false }) {\n                Text(\"Save\")\n            }\n        } else {\n            // View mode\n            Text(text = \"Name: $name\", fontSize = 18.sp)\n            Text(text = \"Age: $age\", fontSize = 18.sp)\n            Button(onClick = { isEditing = true }) {\n                Text(\"Edit\")\n            }\n        }\n    }\n}\n\n// ---- State with objects ----\ndata class UserState(\n    val name: String = \"\",\n    val age: Int = 0,\n    val isLoggedIn: Boolean = false\n)\n\n@Composable\nfun UserStateExample() {\n    var userState by remember { mutableStateOf(UserState()) }\n\n    Column(\n        modifier = Modifier.padding(16.dp)\n    ) {\n        Text(text = \"Name: ${userState.name}\")\n        Text(text = \"Age: ${userState.age}\")\n        Text(text = \"Logged in: ${userState.isLoggedIn}\")\n\n        Button(\n            onClick = {\n                userState = userState.copy(\n                    name = \"Kotlin-chan\",\n                    age = 25,\n                    isLoggedIn = true\n                )\n            }\n        ) {\n            Text(\"Update\")\n        }\n\n        Button(\n            onClick = { userState = UserState() }\n        ) {\n            Text(\"Reset\")\n        }\n    }\n}\n\n// ---- Derived state ----\n@Composable\nfun DerivedStateExample() {\n    var count by remember { mutableStateOf(0) }\n    val isEven = count % 2 == 0\n    val parityText = if (isEven) \"Even\" else \"Odd\"\n\n    Column(\n        modifier = Modifier.padding(16.dp)\n    ) {\n        Text(text = \"Count: $count ($parityText)\")\n        Text(\n            text = \"Is even: $isEven\",\n            color = if (isEven) Color.Green else Color.Red\n        )\n        Button(onClick = { count++ }) {\n            Text(\"Increment\")\n        }\n    }\n}\n\n// ---- State with rememberSaveable ----\n@Composable\nfun SaveableStateExample() {\n    var text by rememberSaveable { mutableStateOf(\"\") }\n\n    Column(\n        modifier = Modifier.padding(16.dp)\n    ) {\n        Text(text = \"Enter some text (survives configuration changes)\")\n        OutlinedTextField(\n            value = text,\n            onValueChange = { text = it },\n            label = { Text(\"Text\") }\n        )\n        Text(text = \"You entered: $text\")\n    }\n}\n\n// ---- State hoisting ----\n@Composable\nfun StatelessCounter(\n    count: Int,\n    onIncrement: () -> Unit,\n    onDecrement: () -> Unit\n) {\n    Column(\n        modifier = Modifier.padding(16.dp),\n        horizontalAlignment = Alignment.CenterHorizontally\n    ) {\n        Text(\n            text = \"Count: $count\",\n            fontSize = 24.sp\n        )\n\n        Row(\n            horizontalArrangement = Arrangement.spacedBy(8.dp)\n        ) {\n            Button(onClick = onDecrement) {\n                Text(\"-\")\n            }\n            Button(onClick = onIncrement) {\n                Text(\"+\")\n            }\n        }\n    }\n}\n\n@Composable\nfun StatefulCounter() {\n    var count by remember { mutableStateOf(0) }\n    StatelessCounter(\n        count = count,\n        onIncrement = { count++ },\n        onDecrement = { count-- }\n    )\n}",
    "output": "// ---- Counter ----\n┌─────────────────────┐\n│   Count: 42          │\n│   [−]  [+]  [Reset] │\n└─────────────────────┘\n\n// ---- UserProfile ----\n┌─────────────────────┐\n│  Name: Kotlin-chan   │\n│  Age: 25             │\n│  [Edit]              │\n└─────────────────────┘\n\n// ---- DerivedState ----\nCount: 3 (Odd)\nIs even: false [Red]\n[Increment]\n\n// ---- StatefulCounter ----\nCount: 5\n[−] [+]",
    "explanation": "🤖 **State and remember manage UI state in Compose.**\n\n**State management:**\n\n| Concept | Code |\n|---------|------|\n| Mutable state | `mutableStateOf(value)` |\n| Remember | `remember { mutableStateOf(value) }` |\n| RememberSaveable | `rememberSaveable { mutableStateOf(value) }` |\n| Derived state | `val derived = state > 0` |\n\n**Kotlin-chan says:**\n\"State drives UI. Recomposition updates UI. It's a loop.\"\n\n**State types:**\n\n| Type | Use case |\n|------|----------|\n| `remember` | UI state that survives recomposition |\n| `rememberSaveable` | Survives configuration changes |\n| `derivedStateOf` | State derived from other state |\n| `mutableStateListOf` | Mutable list state |\n\n**State hoisting:**\n\n| Pattern | Description |\n|---------|-------------|\n| Stateless | Receives state as parameter |\n| Stateful | Owns and manages state |\n| Hoisting | Move state up for reusability |\n\n**Best practices:**\n1. Hoist state when possible\n2. Use rememberSaveable for persisted state\n3. Keep state minimal\n4. Use derived state for computed values\n\n**Kotlin-chan says:**\n\"State hoisting makes your composables reusable and testable.\"",
    "openingDialogue": "State and remember. The heart of Compose UI. 🤖"
  },
  "phase2": {
    "brokenCode": "@Composable\nfun BrokenState() {\n    // 1. State without remember\n    var count = mutableStateOf(0)  // Resets on recomposition!\n\n    // 2. Mutating state directly\n    var name by remember { mutableStateOf(\"\") }\n    name = name + \"a\"  // This triggers recomposition, but inefficient\n\n    // 3. State in non-Composable\n    fun regularFunction() {\n        var state by mutableStateOf(0)  // Error!\n    }\n\n    // 4. Forgetting to use by\n    var count2 = remember { mutableStateOf(0) }\n    Text(\"Count: ${count2.value}\")  // Need .value\n\n    // 5. State from multiple sources\n    var text1 by remember { mutableStateOf(\"\") }\n    var text2 by remember { mutableStateOf(\"\") }\n    // Both cause recomposition independently\n}",
    "errorMessage": "1. State resets on every recomposition\n2. Updating state in composition is inefficient\n3. mutableStateOf can only be used in Composable context\n4. Need to use .value or by delegate\n5. Multiple states cause multiple recompositions",
    "explanation": "🤖 **Common state mistakes and how to fix them.**\n\n| Mistake | Fix |\n|---------|-----|\n| State without remember | Use `remember { mutableStateOf() }` |\n| Direct mutation | Use `by` delegate or `.value` |\n| State in non-Composable | Only use in Composable |\n| Forgetting `.value` | Use `by` delegate |\n| Too many states | Use data class or combine |\n\n**Fixes:**\n```kotlin\n// 1. Use remember\nvar count by remember { mutableStateOf(0) }\n\n// 2. Use by delegate\nvar name by remember { mutableStateOf(\"\") }\nname = \"new\"\n\n// 3. Only in Composable\n@Composable\nfun MyComposable() {\n    var state by remember { mutableStateOf(0) }\n}\n\n// 4. Use by delegate\nvar count by remember { mutableStateOf(0) }\nText(\"Count: $count\")\n\n// 5. Combine states\ndata class UiState(val text1: String, val text2: String)\nvar uiState by remember { mutableStateOf(UiState(\"\", \"\")) }\n```\n\n**Kotlin-chan says:**\n\"State is powerful but use it wisely. Keep it minimal and hoisted.\""
  },
  "phase3": {
    "prompt": "Create a todo list app with state management:\n\nFeatures:\n1. Display a list of todo items\n2. Add new items with a text field and button\n3. Toggle completion status with a checkbox\n4. Show count of completed items\n\nState:\n- `items: List<TodoItem>`\n- `newItemText: String`\n\nData class: `TodoItem(val id: Int, val text: String, val isCompleted: Boolean)`\n\nRequirements:\n- Use `remember` for state\n- Use `mutableStateListOf` for the list\n- Show derived state for completed count\n- Include preview",
    "validationPattern": {
      "required": ["remember", "mutableStateListOf", "mutableStateOf", "TodoItem", "Checkbox"],
      "forbidden": [";"],
      "fullRegex": "remember\\s*\\{[\\s\\S]*mutableStateListOf\\s*\\([\\s\\S]*mutableStateOf\\s*\\([\\s\\S]*data\\s+class\\s+TodoItem[\\s\\S]*Checkbox\\s*\\("
    },
    "dialogueHints": [
      "`data class TodoItem(val id: Int, val text: String, val isCompleted: Boolean)`",
      "`var items by remember { mutableStateListOf<TodoItem>() }`",
      "`var newText by remember { mutableStateOf(\"\") }`",
      "`val completedCount = items.count { it.isCompleted }`",
      "`fun addItem() { items.add(TodoItem(items.size + 1, newText, false)); newText = \"\" }`",
      "`Checkbox(checked = item.isCompleted, onCheckedChange = { items[index] = item.copy(isCompleted = it) })`"
    ],
    "solution": "data class TodoItem(val id: Int, val text: String, val isCompleted: Boolean = false)\n\n@Composable\nfun TodoList() {\n    var items by remember { mutableStateListOf<TodoItem>() }\n    var newText by remember { mutableStateOf(\"\") }\n    val completedCount = items.count { it.isCompleted }\n\n    Column(\n        modifier = Modifier\n            .fillMaxSize()\n            .padding(16.dp)\n    ) {\n        Text(\n            text = \"Todo List\",\n            fontSize = 24.sp,\n            fontWeight = FontWeight.Bold\n        )\n\n        Spacer(modifier = Modifier.height(8.dp))\n\n        Text(\n            text = \"Completed: $completedCount / ${items.size}\",\n            fontSize = 16.sp\n        )\n\n        Spacer(modifier = Modifier.height(16.dp))\n\n        Row(\n            modifier = Modifier.fillMaxWidth(),\n            horizontalArrangement = Arrangement.spacedBy(8.dp)\n        ) {\n            OutlinedTextField(\n                value = newText,\n                onValueChange = { newText = it },\n                label = { Text(\"New todo\") },\n                modifier = Modifier.weight(1f)\n            )\n            Button(\n                onClick = {\n                    if (newText.isNotBlank()) {\n                        items.add(TodoItem(items.size + 1, newText))\n                        newText = \"\"\n                    }\n                }\n            ) {\n                Text(\"Add\")\n            }\n        }\n\n        Spacer(modifier = Modifier.height(16.dp))\n\n        if (items.isEmpty()) {\n            Text(\n                text = \"No todos yet. Add one above!\",\n                color = Color.Gray\n            )\n        } else {\n            items.forEachIndexed { index, item ->\n                Row(\n                    modifier = Modifier\n                        .fillMaxWidth()\n                        .padding(vertical = 4.dp),\n                    verticalAlignment = Alignment.CenterVertically\n                ) {\n                    Checkbox(\n                        checked = item.isCompleted,\n                        onCheckedChange = {\n                            items[index] = item.copy(isCompleted = it)\n                        }\n                    )\n                    Text(\n                        text = item.text,\n                        fontSize = 16.sp,\n                        modifier = Modifier.padding(start = 8.dp)\n                    )\n                }\n            }\n        }\n    }\n}\n\n@Preview(showBackground = true)\n@Composable\nfun PreviewTodoList() {\n    TodoList()\n}",
    "openingDialogue": "State and remember. Build a todo list. 🤖"
  },
  "xpReward": 10
}