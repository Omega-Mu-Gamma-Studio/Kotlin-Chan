{
  "id": "3.8",
  "title": "Theming and Color Schemes",
  "type": "coding",
  "phase1": {
    "code": "// ---- Custom theme ----\n@Composable\nfun CustomTheme() {\n    // Define custom colors\n    val customColors = lightColorScheme(\n        primary = Color(0xFF6200EE),\n        onPrimary = Color.White,\n        primaryContainer = Color(0xFFD1C4E9),\n        onPrimaryContainer = Color(0xFF1A0033),\n        secondary = Color(0xFF03DAC6),\n        onSecondary = Color.Black,\n        secondaryContainer = Color(0xFFB2DFDB),\n        onSecondaryContainer = Color(0xFF004D40),\n        tertiary = Color(0xFFFFC107),\n        onTertiary = Color.Black,\n        background = Color(0xFFF5F5F5),\n        onBackground = Color(0xFF1A1A1A),\n        surface = Color.White,\n        onSurface = Color(0xFF1A1A1A)\n    )\n\n    MaterialTheme(\n        colorScheme = customColors,\n        typography = Typography(\n            headlineLarge = TextStyle(\n                fontSize = 32.sp,\n                fontWeight = FontWeight.Bold,\n                color = Color(0xFF6200EE)\n            ),\n            bodyLarge = TextStyle(\n                fontSize = 16.sp,\n                fontWeight = FontWeight.Normal\n            ),\n            bodySmall = TextStyle(\n                fontSize = 12.sp,\n                color = Color.Gray\n            )\n        )\n    ) {\n        // Your composables here\n    }\n}\n\n// ---- Theme usage example ----\n@Composable\nfun ThemeUsage() {\n    Surface(\n        modifier = Modifier\n            .fillMaxSize()\n            .padding(16.dp),\n        color = MaterialTheme.colorScheme.background\n    ) {\n        Column(\n            verticalArrangement = Arrangement.spacedBy(16.dp)\n        ) {\n            // Using theme colors\n            Text(\n                text = \"Primary Color\",\n                color = MaterialTheme.colorScheme.primary,\n                style = MaterialTheme.typography.headlineLarge\n            )\n\n            Text(\n                text = \"On Primary\",\n                color = MaterialTheme.colorScheme.onPrimary,\n                modifier = Modifier\n                    .background(MaterialTheme.colorScheme.primary)\n                    .padding(8.dp)\n            )\n\n            Text(\n                text = \"Secondary Color\",\n                color = MaterialTheme.colorScheme.secondary,\n                style = MaterialTheme.typography.bodyLarge\n            )\n\n            // Using Material components with theme\n            Card(\n                modifier = Modifier.fillMaxWidth(),\n                colors = CardDefaults.cardColors(\n                    containerColor = MaterialTheme.colorScheme.primaryContainer\n                )\n            ) {\n                Text(\n                    text = \"Primary Container\",\n                    color = MaterialTheme.colorScheme.onPrimaryContainer,\n                    modifier = Modifier.padding(16.dp)\n                )\n            }\n\n            Button(\n                onClick = {},\n                colors = ButtonDefaults.buttonColors(\n                    containerColor = MaterialTheme.colorScheme.primary,\n                    contentColor = MaterialTheme.colorScheme.onPrimary\n                )\n            ) {\n                Text(\"Themed Button\")\n            }\n\n            OutlinedButton(\n                onClick = {},\n                colors = ButtonDefaults.outlinedButtonColors(\n                    contentColor = MaterialTheme.colorScheme.primary\n                )\n            ) {\n                Text(\"Outlined Themed\")\n            }\n        }\n    }\n}\n\n// ---- Dynamic color (Android 12+) ----\n@Composable\nfun DynamicColorTheme(\n    darkTheme: Boolean = isSystemInDarkTheme(),\n    content: @Composable () -> Unit\n) {\n    val colorScheme = if (darkTheme) {\n        dynamicDarkColorScheme(LocalContext.current)\n    } else {\n        dynamicLightColorScheme(LocalContext.current)\n    }\n\n    MaterialTheme(\n        colorScheme = colorScheme ?: if (darkTheme) darkColorScheme() else lightColorScheme(),\n        content = content\n    )\n}\n\n// ---- Dark/Light theme toggle ----\n@Composable\nfun ThemeToggleExample() {\n    var isDarkTheme by remember { mutableStateOf(false) }\n\n    // Choose color scheme based on theme\n    val colorScheme = if (isDarkTheme) {\n        darkColorScheme(\n            primary = Color(0xFFBB86FC),\n            onPrimary = Color.Black,\n            primaryContainer = Color(0xFF3700B3),\n            onPrimaryContainer = Color(0xFFD1C4E9),\n            secondary = Color(0xFF03DAC6),\n            onSecondary = Color.Black,\n            background = Color(0xFF121212),\n            onBackground = Color(0xFFE0E0E0),\n            surface = Color(0xFF1E1E1E),\n            onSurface = Color(0xFFE0E0E0)\n        )\n    } else {\n        lightColorScheme(\n            primary = Color(0xFF6200EE),\n            onPrimary = Color.White,\n            primaryContainer = Color(0xFFD1C4E9),\n            onPrimaryContainer = Color(0xFF1A0033),\n            secondary = Color(0xFF03DAC6),\n            onSecondary = Color.Black,\n            background = Color(0xFFF5F5F5),\n            onBackground = Color(0xFF1A1A1A),\n            surface = Color.White,\n            onSurface = Color(0xFF1A1A1A)\n        )\n    }\n\n    MaterialTheme(colorScheme = colorScheme) {\n        Surface(\n            modifier = Modifier.fillMaxSize(),\n            color = MaterialTheme.colorScheme.background\n        ) {\n            Column(\n                modifier = Modifier.padding(16.dp),\n                verticalArrangement = Arrangement.spacedBy(16.dp)\n            ) {\n                Text(\n                    text = if (isDarkTheme) \"Dark Mode\" else \"Light Mode\",\n                    style = MaterialTheme.typography.headlineLarge,\n                    color = MaterialTheme.colorScheme.onBackground\n                )\n\n                Text(\n                    text = \"This text adapts to the theme\",\n                    color = MaterialTheme.colorScheme.onBackground\n                )\n\n                Button(\n                    onClick = { isDarkTheme = !isDarkTheme }\n                ) {\n                    Text(\"Toggle Theme\")\n                }\n\n                Card(\n                    colors = CardDefaults.cardColors(\n                        containerColor = MaterialTheme.colorScheme.primaryContainer\n                    )\n                ) {\n                    Text(\n                        text = \"Primary Container\",\n                        color = MaterialTheme.colorScheme.onPrimaryContainer,\n                        modifier = Modifier.padding(16.dp)\n                    )\n                }\n            }\n        }\n    }\n}",
    "output": "// ---- Theme Usage ----\n┌─────────────────────────────────────┐\n│ Primary Color                       │\n│ On Primary                          │\n│ Secondary Color                     │\n│ ┌─────────────────────────────────┐ │\n│ │ Primary Container                │ │\n│ └─────────────────────────────────┘ │\n│ [Themed Button]                     │\n│ [Outlined Themed]                   │\n└─────────────────────────────────────┘\n\n// ---- Dark/Light toggle ----\nLight Mode\nThis text adapts to the theme\n[Toggle Theme]\n┌─────────────────────────────────────┐\n│ Primary Container                    │\n└─────────────────────────────────────┘\n\n// After toggle:\nDark Mode\nThis text adapts to the theme\n[Toggle Theme]\n┌─────────────────────────────────────┐\n│ Primary Container                    │\n└─────────────────────────────────────┘",
    "explanation": "🤖 **Theming and color schemes define your app's visual identity.**\n\n**Color scheme:**\n\n| Property | Purpose |\n|----------|---------|\n| `primary` | Main brand color |\n| `onPrimary` | Text on primary |\n| `primaryContainer` | Container for primary |\n| `secondary` | Secondary brand color |\n| `background` | App background |\n| `surface` | Cards, sheets |\n\n**Kotlin-chan says:**\n\"Theming gives your app a consistent identity.\"\n\n**Theme types:**\n\n| Type | Code |\n|------|------|\n| Light | `lightColorScheme()` |\n| Dark | `darkColorScheme()` |\n| Dynamic | `dynamicColorScheme()` |\n| Custom | `ColorScheme()` |\n\n**Typography:**\n\n| Style | Purpose |\n|-------|---------|\n| `headlineLarge` | Large titles |\n| `bodyLarge` | Main text |\n| `bodySmall` | Secondary text |\n| `labelLarge` | Button text |\n\n**Using theme colors:**\n```kotlin\nMaterialTheme.colorScheme.primary\nMaterialTheme.colorScheme.onPrimary\nMaterialTheme.colorScheme.background\n```\n\n**Dynamic Color (Android 12+):**\n- Uses system accent color\n- Adapts to wallpaper\n- Requires API 31+\n\n**Best practices:**\n1. Define all colors\n2. Use theme colors everywhere\n3. Support dark mode\n4. Test different themes\n5. Use dynamic color when available\n\n**Kotlin-chan says:**\n\"Theming makes your app feel polished. Do it right.\"",
    "openingDialogue": "Theming and color schemes. Define your app's identity. 🤖"
  },
  "phase2": {
    "brokenCode": "@Composable\nfun BrokenTheme() {\n    // 1. Hardcoded colors\n    Text(\n        text = \"Hello\",\n        color = Color.Blue  // Should use theme\n    )\n\n    // 2. Forgetting to wrap in MaterialTheme\n    @Composable\n    fun MyApp() {\n        Text(\"Hello\")  // No theme applied!\n    }\n\n    // 3. Inconsistent colors\n    Button(\n        onClick = {},\n        colors = ButtonDefaults.buttonColors(\n            containerColor = Color.Red  // Doesn't match theme\n        )\n    ) {\n        Text(\"Button\")\n    }\n\n    // 4. Not using onSurface\n    Card(\n        colors = CardDefaults.cardColors(\n            containerColor = Color.Black\n        )\n    ) {\n        Text(\n            text = \"Text\",\n            color = Color.White  // Should use onSurface\n        )\n    }\n\n    // 5. Missing dark theme support\n    @Composable\n    fun MyTheme(content: @Composable () -> Unit) {\n        MaterialTheme(\n            colorScheme = lightColorScheme()  // Only light theme!\n        ) {\n            content()\n        }\n    }\n}",
    "errorMessage": "1. Hardcoded colors don't adapt to theme\n2. Text uses default theme (MaterialTheme missing)\n3. Button color doesn't match theme\n4. Card text color doesn't match theme\n5. No dark theme support",
    "explanation": "🤖 **Common theming mistakes and how to fix them.**\n\n| Mistake | Fix |\n|---------|-----|\n| Hardcoded colors | Use `MaterialTheme.colorScheme.primary` |\n| No MaterialTheme | Wrap all composables in MaterialTheme |\n| Inconsistent colors | Use theme colors everywhere |\n| Wrong text color | Use `onSurface` or `onPrimary` |\n| No dark theme | Support both light and dark |\n\n**Fixes:**\n```kotlin\n// 1. Use theme colors\nText(\n    text = \"Hello\",\n    color = MaterialTheme.colorScheme.primary\n)\n\n// 2. Wrap in MaterialTheme\n@Composable\nfun MyApp() {\n    MaterialTheme {\n        Text(\"Hello\")\n    }\n}\n\n// 3. Theme colors\nButton(\n    onClick = {},\n    colors = ButtonDefaults.buttonColors(\n        containerColor = MaterialTheme.colorScheme.primary\n    )\n) {\n    Text(\"Button\")\n}\n\n// 4. Use onSurface\nCard(\n    colors = CardDefaults.cardColors(\n        containerColor = MaterialTheme.colorScheme.surface\n    )\n) {\n    Text(\n        text = \"Text\",\n        color = MaterialTheme.colorScheme.onSurface\n    )\n}\n\n// 5. Support dark theme\n@Composable\nfun MyTheme(\n    darkTheme: Boolean = isSystemInDarkTheme(),\n    content: @Composable () -> Unit\n) {\n    val colorScheme = if (darkTheme) darkColorScheme() else lightColorScheme()\n    MaterialTheme(colorScheme = colorScheme) {\n        content()\n    }\n}\n```\n\n**Kotlin-chan says:**\n\"Theme colors everywhere. No hardcoded colors.\""
  },
  "phase3": {
    "prompt": "Create a custom theme with:\n\n1. Primary color: #FF6B6B (red-ish)\n2. Secondary color: #4ECDC4 (teal-ish)\n3. Background color: #F7F7F7\n4. Surface color: #FFFFFF\n\nThen create a screen that uses these theme colors:\n- A header with primary color\n- A card with surface color\n- A button with secondary color\n- Text with appropriate on colors\n\nAlso support dark mode.\n\nRequirements:\n- Define custom color scheme\n- Use MaterialTheme\n- Support dark/light toggle\n- Use theme colors in components",
    "validationPattern": {
      "required": ["lightColorScheme", "darkColorScheme", "MaterialTheme", "primary", "secondary"],
      "forbidden": [";"],
      "fullRegex": "lightColorScheme\\s*\\([\\s\\S]*primary\\s*=\\s*Color\\s*\\(0x[0-9A-Fa-f]+\\s*\\)[\\s\\S]*darkColorScheme\\s*\\([\\s\\S]*MaterialTheme\\s*\\([\\s\\S]*colorScheme\\s*="
    },
    "dialogueHints": [
      "`val lightColors = lightColorScheme(primary = Color(0xFFFF6B6B), secondary = Color(0xFF4ECDC4), background = Color(0xFFF7F7F7), surface = Color.White)`",
      "`val darkColors = darkColorScheme(primary = Color(0xFFFF6B6B), secondary = Color(0xFF4ECDC4), background = Color(0xFF1A1A1A), surface = Color(0xFF2D2D2D))`",
      "`MaterialTheme(colorScheme = if (isDark) darkColors else lightColors)`",
      "`Text(text = \"Header\", color = MaterialTheme.colorScheme.primary)`",
      "`Card(colors = CardDefaults.cardColors(containerColor = MaterialTheme.colorScheme.surface))`",
      "`Button(colors = ButtonDefaults.buttonColors(containerColor = MaterialTheme.colorScheme.secondary))`"
    ],
    "solution": "@Composable\nfun CustomThemeScreen() {\n    var isDark by remember { mutableStateOf(false) }\n\n    val lightColors = lightColorScheme(\n        primary = Color(0xFFFF6B6B),\n        onPrimary = Color.White,\n        primaryContainer = Color(0xFFFFCDD2),\n        onPrimaryContainer = Color(0xFFB71C1C),\n        secondary = Color(0xFF4ECDC4),\n        onSecondary = Color.White,\n        secondaryContainer = Color(0xFFB2DFDB),\n        onSecondaryContainer = Color(0xFF004D40),\n        background = Color(0xFFF7F7F7),\n        onBackground = Color(0xFF1A1A1A),\n        surface = Color.White,\n        onSurface = Color(0xFF1A1A1A)\n    )\n\n    val darkColors = darkColorScheme(\n        primary = Color(0xFFFF6B6B),\n        onPrimary = Color.White,\n        primaryContainer = Color(0xFFB71C1C),\n        onPrimaryContainer = Color(0xFFFFCDD2),\n        secondary = Color(0xFF4ECDC4),\n        onSecondary = Color.White,\n        secondaryContainer = Color(0xFF004D40),\n        onSecondaryContainer = Color(0xFFB2DFDB),\n        background = Color(0xFF121212),\n        onBackground = Color(0xFFE0E0E0),\n        surface = Color(0xFF1E1E1E),\n        onSurface = Color(0xFFE0E0E0)\n    )\n\n    val colorScheme = if (isDark) darkColors else lightColors\n\n    MaterialTheme(colorScheme = colorScheme) {\n        Surface(\n            modifier = Modifier.fillMaxSize(),\n            color = MaterialTheme.colorScheme.background\n        ) {\n            Column(\n                modifier = Modifier\n                    .fillMaxSize()\n                    .padding(16.dp),\n                verticalArrangement = Arrangement.spacedBy(16.dp)\n            ) {\n                // Header with primary\n                Text(\n                    text = \"Custom Theme Demo\",\n                    color = MaterialTheme.colorScheme.primary,\n                    fontSize = 28.sp,\n                    fontWeight = FontWeight.Bold\n                )\n\n                // Card with surface\n                Card(\n                    modifier = Modifier.fillMaxWidth(),\n                    colors = CardDefaults.cardColors(\n                        containerColor = MaterialTheme.colorScheme.surface\n                    ),\n                    elevation = CardDefaults.cardElevation(\n                        defaultElevation = 4.dp\n                    )\n                ) {\n                    Column(\n                        modifier = Modifier.padding(16.dp)\n                    ) {\n                        Text(\n                            text = \"Surface Card\",\n                            color = MaterialTheme.colorScheme.primary,\n                            fontSize = 18.sp,\n                            fontWeight = FontWeight.Bold\n                        )\n                        Text(\n                            text = \"This card uses the surface color from the theme.\",\n                            color = MaterialTheme.colorScheme.onSurface\n                        )\n                    }\n                }\n\n                // Secondary button\n                Button(\n                    onClick = { isDark = !isDark },\n                    colors = ButtonDefaults.buttonColors(\n                        containerColor = MaterialTheme.colorScheme.secondary,\n                        contentColor = MaterialTheme.colorScheme.onSecondary\n                    ),\n                    modifier = Modifier.fillMaxWidth()\n                ) {\n                    Text(\"Toggle Theme\")\n                }\n\n                // Primary button\n                Button(\n                    onClick = { /* Action */ },\n                    colors = ButtonDefaults.buttonColors(\n                        containerColor = MaterialTheme.colorScheme.primary,\n                        contentColor = MaterialTheme.colorScheme.onPrimary\n                    ),\n                    modifier = Modifier.fillMaxWidth()\n                ) {\n                    Text(\"Primary Button\")\n                }\n\n                // Current theme info\n                Text(\n                    text = \"Current Theme: ${if (isDark) \"Dark\" else \"Light\"}\",\n                    color = MaterialTheme.colorScheme.onBackground\n                )\n            }\n        }\n    }\n}\n\n@Preview(showBackground = true)\n@Composable\nfun PreviewCustomThemeScreen() {\n    CustomThemeScreen()\n}",
    "openingDialogue": "Theming. Create custom colors. Show me. 🤖"
  },
  "xpReward": 10
}